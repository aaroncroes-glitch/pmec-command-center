import { useMemo } from "react";
import { StyleSheet, Text, View } from "react-native";
import { useLumen } from "@/lib/lumen-workspace";
export function EmptyState({ kicker, title, body }: { kicker: string; title: string; body: string }) { const { palette } = useLumen(); const styles = useMemo(() => StyleSheet.create({ root: { alignItems: "flex-start", borderBottomColor: palette.border, borderBottomWidth: StyleSheet.hairlineWidth, gap: 8, paddingHorizontal: 22, paddingVertical: 30 }, kicker: { color: palette.accentText, fontSize: 10, fontWeight: "900", letterSpacing: 1.1 }, title: { color: palette.foreground, fontSize: 24, fontWeight: "900", letterSpacing: -0.8 }, body: { color: palette.muted, fontSize: 14, lineHeight: 20, maxWidth: 280 } }), [palette]); return <View style={styles.root}><Text style={styles.kicker}>{kicker}</Text><Text style={styles.title}>{title}</Text><Text style={styles.body}>{body}</Text></View>; }
