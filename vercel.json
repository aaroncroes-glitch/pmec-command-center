{
  "buildCommand": "pnpm export:web",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/__clerk/:path*",
      "destination": "https://clerk.portal.pmec.group/:path*"
    },
    {
      "source": "/api/:path*",
      "destination": "/api/[...path]"
    },
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
